#!/usr/local/bin/perl
use strict;
use warnings;
use DBI;

# This script will be used for the fast emptying/deletion of experiments
# in the TRAPID database. The PHP 
print STDOUT "Starting with the database tools (empty/delete) \n";
