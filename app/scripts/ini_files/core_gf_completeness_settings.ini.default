; Currently, the only configuration variable for the core GF completeness script is the path of the NCBI taxonomy
; sqlite file generated by ETE (used when working with PLAZA databases to avoid relying on TRAPID DB).
; Ensure that the taxonomy data is consistent between Kaiju, TRAPID/reference DBs, and ETE
; TODO: add the rest of the parameters to the INI file.
[core_gf]
ete_ncbi_dbfile = "/path/to/.etetoolkit/taxa.sqlite"
